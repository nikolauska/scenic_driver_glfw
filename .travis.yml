language: elixir
elixir: '1.7.3'
otp_release: '21.0'

addons:
  apt:
    packages:
      - make
      - gcc
      - glfw3-dev

install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

script:
  - mix compile --warnings-as-errors
